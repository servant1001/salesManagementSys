# AGENTS.md

本檔案定義此專案後續開發時應遵循的 UI 系統與實作規則。

## 適用範圍

除非使用者明確要求不同方向，否則所有新的 UI 工作都應套用這份規則。

## 目前的 UI 方向

目前的視覺語言是乾淨、偏零售後台風格的管理介面，特徵如下：

- 深藍色背景
- 暖金色點綴
- 柔和的玻璃感 / 卡片式表面
- 圓角設計
- 清楚的留白與間距
- 以實用、易讀的版面優先，而不是裝飾性過強的設計

整體目標是「精緻的內部工具」，不是「通用模板感」也不是「消費型行銷頁」。

## 核心視覺規則

### 色彩

請以 `src/assets/main.css` 內既有的 CSS 變數系統作為唯一色彩來源。

偏好的色彩方向：

- 背景：深海軍藍、石板藍、柔和冷灰
- 點綴：暖金 / 古銅色
- 主要操作：藍色漸層按鈕
- 危險操作：低飽和紅色，不要螢光感

避免：

- 偏紫色系主導的配色
- 平白頁面加上預設 Element Plus 樣式
- 在工作頁面後方使用過度複雜的照片背景
- 跳過 token 系統直接使用隨機色彩

### 表面與卡片

大多數內容頁應採用分層卡片結構：

- 大型 hero / 概要卡
- 次要控制區卡片
- 主要內容或表格卡片

目前專案已使用的圓角尺度：

- 14px：輸入框與按鈕
- 16px 到 18px：小型卡片 / 控制元件
- 22px 到 28px：主要卡片

### 字體與文案

語氣應該偏營運管理、冷靜且清楚。

- 使用簡潔的繁體中文作為主要介面文案
- 小型英文 eyebrow label 只在適合時作為輔助點綴
- 標題要有力量但保持簡潔
- 資料型頁面避免過長、過度行銷感的文案

### 表單樣式

表單應延續登入頁的視覺語言：

- 圓角輸入框
- 柔和的 inset 邊框
- 暖金色 focus ring
- 明確的主要按鈕
- 使用柔和表面樣式的次要按鈕

參考檔案：

- `src/views/Login.vue`
- `src/views/Register.vue`

## 版面規則

### App Shell

整體 app shell 使用：

- 深色側邊欄
- 由主題變數控制的淺 / 深內容背景
- 內容區內部使用圓角視覺區塊

參考檔案：

- `src/layouts/MainLayout.vue`

調整 sidebar 時：

- 優先確保對齊穩定，而不是追求過度激進的改版
- 收合與展開狀態都要保持簡潔
- 不要讓裝飾元素蓋住核心導覽
- 在加入新的視覺效果前，先確保手機版可用

### Dashboard / 內容頁

若頁面性質適合，優先採用以下結構：

1. Hero / 摘要卡
2. 控制 / 篩選卡
3. 主要內容卡

參考檔案：

- `src/views/HomeView.vue`
- `src/views/Vendors.vue`

## Table 規則

表格是這個專案最主要的資料承載表面，請特別小心處理。

桌機版行為：

- 優先讓整頁能在視窗內合理呈現
- 優先使用表格內部滾動，而不是讓整頁變得過長
- 水平溢位必須被包在表格容器內，不可以把整個頁面撐壞

手機版行為：

- 不要因固定高度把整頁鎖死，導致內容看不到
- 手機上允許頁面本身可以正常上下捲動
- 表格本身允許水平捲動
- 若表格在手機上過於密集，不要硬塞完整表格，應考慮改成卡片式列表

對於像 vendor 這種資料頁：

- 篩選控制列要能透過 wrapping flex 響應式換行
- 不要假設只有桌機版固定 grid 佈局

## 響應式規則

- 可以桌機優先設計，但每一頁都必須保證手機可用
- 優先使用 `flex-wrap`，避免脆弱的固定寬度控制列
- 除非已經仔細測試，否則避免在手機上大量使用硬編碼 viewport 高度計算
- 若桌機版使用固定高度表格，手機版要有獨立 fallback
- 每次都要檢查：
  - 手機版側欄展開
  - 桌機版側欄收合
  - 長表格寬度溢位
  - 手機瀏覽器 chrome 對頁面高度的影響

## 實作規則

- 盡量重用 `src/assets/main.css` 裡的變數
- 優先沿用 Element Plus 元件，不要替換整個 UI 技術棧
- 樣式優先寫在頁面 / 元件的 scoped 區塊，除非那真的是全域 token
- 優先延伸既有設計語言，不要每頁重新發明一套風格
- 若某頁已經符合新風格，就以它為基準，不要再做出第三種版本

## 已經切到新風格的頁面

以下檔案目前已是這套設計語言的基準，後續應以它們為參考：

- `src/views/Login.vue`
- `src/views/Register.vue`
- `src/layouts/MainLayout.vue`
- `src/views/HomeView.vue`
- `src/views/Vendors.vue`
- `src/assets/main.css`

## 不要重新引入

- 幾乎沒設計過的預設 Element Plus 頁面
- 在工作頁面使用滿版素材照片背景
- 因寬表格導致整頁失控溢位
- 不一致的按鈕語言與操作文案
- 因固定 viewport 高度導致手機版壞掉
- 介面文案亂碼 / 編碼錯誤

## 未來 UI 工作的建議流程

開始修改前：

1. 先讀這份檔案
2. 找最接近需求的參考頁
3. 重用現有 token 與 spacing 語言

修改完成後：

1. 檢查桌機版版面
2. 檢查手機版版面
3. 如果頁面包含表格，檢查 overflow 行為
4. 執行 `npm run build`

## 發生取捨時的優先順序

如果風格與穩定性衝突，請依照以下順序取捨：

1. 可用性
2. 版面穩定性
3. 響應式正確性
4. 視覺精緻度
5. 額外裝飾效果

這個專案應該看起來有設計感且現代，但不能脆弱。
