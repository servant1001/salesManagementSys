# AGENTS.md

本文件用來記錄此專案目前採用的 UI / UX 設計語言、配色規範、元件風格與後續延伸原則，方便在不同電腦或不同開發階段持續沿用同一套視覺系統。

## 專案 UI / UX 核心方向

此專案目前採用「現代商務後台 + 藍金質感」的介面風格，目標是讓銷售、商品、結帳、分析等功能頁面在桌機與手機上都維持一致、清楚、可操作的體驗。

- 主視覺以深藍作為穩定基底，金色作為強調與品牌感點綴。
- 介面結構以 `Hero / Card / Table / Dialog` 為核心組成。
- 優先保留資訊密度，但避免傳統後台過於死板、擁擠的排版。
- 重點操作按鈕需要有層級區分，避免所有按鈕看起來一樣重要。
- 手機版需保有可操作性，避免內容溢出、按鈕錯位、無法捲動或輸入框高度不一致。

## 設計語言

### 1. 頁面結構

頁面建議遵循以下層次：

1. Hero 區塊
2. 功能控制區 / 篩選區 / 摘要區
3. 主要資料區塊，例如 Table、分析卡片、列表
4. Dialog / 明細視窗 / 編輯視窗

### 2. 卡片風格

- 卡片圓角建議落在 `22px` 到 `30px`
- 卡片需保留明顯留白，不要緊貼內容邊界
- 卡片內距建議為 `18px` 到 `28px`
- 卡片應搭配柔和陰影，而不是厚重黑影

### 3. 表格風格

- 表格應保留清楚的欄位分區與表頭層次
- 表頭需與內容區有足夠對比
- 資料狀態可搭配 badge、數值色彩或小型操作按鈕輔助辨識
- 行內操作不應過寬，避免壓縮主要資料欄位
- 表格區應可在內容過多時獨立捲動，不讓整頁產生多重滾動體驗

### 4. 表單與操作體驗

- 輸入框、日期、下拉選單高度應盡量一致
- 同一列的控制項需對齊，不可出現某些元件特別高或特別矮
- 刪除、儲存、確認等按鈕要有明確語意色彩
- 避免受到 Element Plus 預設樣式干擾，例如按鈕間距或手機版 `margin-left`

## 按鈕設計系統

按鈕目前已整理成明確層級，後續新增按鈕時請盡量沿用，不要重新發明新風格。

### 1. 主要操作按鈕 Primary

用途：

- 新增商品
- 加入購物車
- 開啟主要流程
- Dialog 主確認按鈕

風格原則：

- 深藍漸層底
- 白色文字
- 有輕微浮起陰影
- Hover 時可微幅上浮 `translateY(-1px)`

建議色碼：

- 漸層起點 `#112842`
- 漸層終點 `#183656`
- 深層 hover `#0e223a`
- 邊框強調 `#d6a46b`
- 文字 `#f7fbff`

對應範例 class：

- `checkout-primary-btn`

### 2. 強調確認按鈕 Confirm / Success CTA

用途：

- 確認結帳
- 開始掃描
- 需要特別醒目的關鍵操作

風格原則：

- 金橘色漸層
- 深藍文字
- 陰影比 Primary 更明顯
- 必須一眼看出是主流程的關鍵按鈕

建議色碼：

- 漸層起點 `#be6825`
- 漸層中段 `#df9c45`
- 漸層終點 `#f4c87d`
- 次級金色版本 `#b97837`
- 柔和金色版本 `#d6a46b`
- 高光金色版本 `#f0c998`
- 文字 `#10243c`

對應範例 class：

- `checkout-success-btn`
- `scanner-toggle-btn`

### 3. 次要按鈕 Secondary

用途：

- 取消
- 清空購物車
- 手動建立商品
- 次要功能切換

風格原則：

- 淺色底
- 深藍文字
- 細邊框
- 不搶主按鈕風頭，但仍要維持質感

建議色碼：

- 背景 `#ffffff`
- hover 背景 `#f7fafc`
- 文字 `#10243c`
- 邊框基準 `rgba(20, 36, 58, 0.10)`
- hover 邊框 `rgba(77, 131, 180, 0.28)`

對應範例 class：

- `checkout-secondary-btn`

### 4. 行內操作按鈕 Row Actions

用途：

- 編輯
- 儲存
- 刪除
- 列表操作

風格原則：

- 尺寸要比主按鈕更精簡
- 可垂直排列，減少橫向佔位
- 用顏色區分語意，但不要過度刺眼

編輯 / 儲存按鈕色碼：

- 起點 `#183656`
- 終點 `#234a71`
- 文字 `#f7fbff`

刪除按鈕色碼：

- 起點 `#bf4848`
- 終點 `#aa3a3a`
- hover 起點 `#b03c3c`
- hover 終點 `#9b3030`
- 文字 `#fff8f8`

對應範例 class：

- `checkout-row-btn`
- `checkout-row-btn--edit`
- `checkout-row-btn--delete`

### 5. 禁用狀態 Disabled

禁用按鈕不可沿用 Element Plus 預設綠色或其他與當前設計不一致的色彩。

風格原則：

- 使用灰藍低對比底色
- 取消 hover 浮起效果
- 保留可辨識的不可點擊狀態

建議色碼：

- 背景上層 `#e7ecf1`
- 背景下層 `#dbe2e9`
- 文字 `rgba(16, 36, 60, 0.48)`
- 邊框 `rgba(20, 36, 58, 0.10)`

目前已套用案例：

- `checkout-success-btn:disabled`

## 色彩系統

以下色碼為目前專案主要使用中的設計 token，可優先沿用。

### 深藍主色系

- `#091626`
- `#0d2037`
- `#10243c`
- `#112842`
- `#173255`
- `#183656`
- `#21456d`

### 金色強調色系

- `#b97837`
- `#be6825`
- `#cf9d6a`
- `#d6a46b`
- `#df9c45`
- `#f0c998`
- `#f4c87d`

### 中性色與文字色

- `#f3f7fc`
- `#f4f8fc`
- `#f5f7fb`
- `#f5f8fc`
- `#e4ebf3`
- `#d9e2ef`
- `#95a7bb`
- `#72859a`
- `#32465b`

### 功能色

- 成功 `#1d8f50`
- 成功輔助 `#67c23a`
- 錯誤 / 刪除 `#d94e4e`
- 錯誤深色 `#b63e3e`
- 連結 `#2f6fa8`

## 元件補充規範

### 輸入框與選擇器

- `el-input`、`el-select`、`el-date-editor`、`el-input-number` 高度需盡量一致
- Focus ring 優先使用金色系，不使用預設藍紫色
- 建議 focus 強調色使用 `#b97837`

### Dialog

- 標題列盡量簡潔，不要使用厚重實色底
- 以字重、留白、內容分區來建立層次
- 深色模式下避免標題與背景融在一起

### 手機版

- 手機模式下按鈕若改為直向堆疊，需清除 `el-button + el-button` 的預設左側間距
- 日期、輸入框、下拉選單寬度需一致
- 表格若超出畫面，需提供區塊內捲動，不可讓整頁卡死
- 側邊欄與主內容需分離滾動行為，避免左側跟著右側一起跑版

## 已套用這套風格的檔案

- `src/views/Login.vue`
- `src/views/Register.vue`
- `src/layouts/MainLayout.vue`
- `src/views/HomeView.vue`
- `src/views/Vendors.vue`
- `src/components/ProductList.vue`
- `src/components/CheckoutRedesign.vue`
- `src/components/Scanner.vue`
- `src/components/SalesRecord.vue`
- `src/assets/main.css`

## 開發延續原則

- 新頁面優先沿用現有 Hero、Card、Table、Dialog 語言
- 新按鈕優先從既有 Primary / Secondary / Confirm / Row Action 衍生
- 若新增顏色，必須先確認是否真的無法用現有色系解決
- 若使用 Element Plus 新元件，需檢查是否帶入預設間距、預設色彩或手機版跑版問題
- 若完成較大幅度調整，請至少執行一次 `npm run build`

## 備註

- 舊版元件仍可能保留在專案中，例如 `src/components/Checkout.vue`、`src/components/Sales.vue`
- 目前實際上線視覺以 `CheckoutRedesign.vue`、`SalesRecord.vue` 等新版元件為主
- 若後續新增頁面，請先閱讀本文件，再決定是否要擴充這套設計系統
